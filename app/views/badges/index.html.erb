<h1><%= t('.header') %></h1>

<table class="table">
  <thead>
    <tr>
      <th><%= t('.name') %></th>
      <th><%= t('.description') %></th>
      <th><%= t('.icon') %></th>
      <th><%= t('admin.tests.show.category') %></th>
      <th><%= t('admin.tests.show.hard') %></th>
    </tr>
  </thead>

  <tbody>
    <% @badges.each do |badge| %>
      <%= render badge %>
    <% end %>
  </tbody>
</table>
