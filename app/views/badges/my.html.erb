<h1><%= t('.header') %></h1>

<table class="table">
  <thead>
    <tr>
      <th><%= t('badges.index.name') %></th>
      <th><%= t('badges.index.description') %></th>
      <th><%= t('badges.index.icon') %></th>
    </tr>
  </thead>

  <tbody>
    <% @badges.each do |badge| %>
      <%= render badge %>
    <% end %>
  </tbody>
</table>
