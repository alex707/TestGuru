<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'Test Guru', tests_path, class: 'navbar-brand' %>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <%= link_to 'Badges',
                    badges_index_path,
                    class: 'nav-link dropdown-toggle',
                    id: 'navbarDropdown',
                    data: { toggle: 'dropdown' },
                    role: 'button',
                    aria: { expanded: 'false' }
        %>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <%= link_to 'All', badges_index_path, class: 'dropdown-item' %>
          <%= link_to 'My', badges_my_path, class: 'dropdown-item' %>
        </div>
      </li>
    </ul>
  </div>

  <span class="navbar-text">
    <% if user_signed_in? %>
      <%= t('.welcome') %>, <b><%= show_name %></b>Guru |
      <%= current_user.admin? ? link_to(t('.admin_panel'), admin_tests_path) : link_to(t('feedback.new.header'), feedback_new_path) %> |
      <%= link_to(t('.log_out'), destroy_user_session_path, method: :delete) %>
    <% else %>
      <%= link_to(t('.log_in'), user_session_path) %> |
      <%= link_to(t('.sign_up'), new_user_registration_path) %>
    <% end %>
  </span>
</nav>
